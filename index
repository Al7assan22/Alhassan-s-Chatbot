<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
<meta name="mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<title>Alhassan's Chatbot</title>
<link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@300;400;500;700&family=Orbitron:wght@400;700;900&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #0a0c10;
    --surface: #0f1318;
    --surface2: #141920;
    --border: #1e2730;
    --accent: #00d4ff;
    --accent2: #00ff88;
    --accent3: #ff6b35;
    --text: #c8d8e8;
    --text-dim: #4a6070;
    --text-muted: #2a3a4a;
    --user-bubble: #0d2235;
    --bot-bubble: #0d1f18;
    --glow: rgba(0,212,255,0.15);
    --glow2: rgba(0,255,136,0.1);
  }

  * { box-sizing: border-box; margin: 0; padding: 0; }

  body {
    background: var(--bg);
    color: var(--text);
    font-family: 'JetBrains Mono', monospace;
    height: 100vh;
    display: flex;
    flex-direction: column;
    overflow: hidden;
    position: relative;
  }

  /* Animated grid background */
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background-image:
      linear-gradient(rgba(0,212,255,0.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(0,212,255,0.03) 1px, transparent 1px);
    background-size: 40px 40px;
    pointer-events: none;
    z-index: 0;
  }

  /* Scan line effect */
  body::after {
    content: '';
    position: fixed;
    inset: 0;
    background: repeating-linear-gradient(
      0deg,
      transparent,
      transparent 2px,
      rgba(0,0,0,0.05) 2px,
      rgba(0,0,0,0.05) 4px
    );
    pointer-events: none;
    z-index: 0;
  }

  /* Header */
  .header {
    position: relative;
    z-index: 10;
    background: var(--surface);
    border-bottom: 1px solid var(--border);
    padding: 16px 24px;
    display: flex;
    align-items: center;
    gap: 16px;
    box-shadow: 0 0 30px rgba(0,212,255,0.08);
  }

  .avatar {
    width: 52px;
    height: 52px;
    border-radius: 14px;
    background: #0d1a1f;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    flex-shrink: 0;
    overflow: hidden;
    border: 1px solid rgba(0,212,255,0.3);
  }

  .avatar::after {
    content: '';
    position: absolute;
    inset: -3px;
    border-radius: 14px;
    border: 1px solid var(--accent);
    opacity: 0.5;
    animation: pulse-border 2s ease-in-out infinite;
  }

  @keyframes pulse-border {
    0%, 100% { opacity: 0.3; transform: scale(1); }
    50% { opacity: 0.8; transform: scale(1.05); }
  }

  .header-info { flex: 1; }

  .bot-name {
    font-family: 'Orbitron', monospace;
    font-size: 16px;
    font-weight: 700;
    color: var(--accent);
    letter-spacing: 2px;
    text-transform: uppercase;
  }

  .bot-status {
    font-size: 11px;
    color: var(--accent2);
    margin-top: 2px;
    display: flex;
    align-items: center;
    gap: 6px;
  }

  .status-dot {
    width: 6px;
    height: 6px;
    background: var(--accent2);
    border-radius: 50%;
    animation: blink 1.5s ease-in-out infinite;
    box-shadow: 0 0 6px var(--accent2);
  }

  @keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.3; }
  }

  .header-tags {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
  }

  .tag {
    font-size: 9px;
    padding: 3px 8px;
    border-radius: 4px;
    border: 1px solid;
    font-family: 'JetBrains Mono', monospace;
    letter-spacing: 1px;
    text-transform: uppercase;
  }

  .tag-kafka { color: #ff6b35; border-color: rgba(255,107,53,0.4); background: rgba(255,107,53,0.08); }
  .tag-spark { color: #ffd700; border-color: rgba(255,215,0,0.4); background: rgba(255,215,0,0.08); }
  .tag-python { color: #00d4ff; border-color: rgba(0,212,255,0.4); background: rgba(0,212,255,0.08); }
  .tag-ai { color: #00ff88; border-color: rgba(0,255,136,0.4); background: rgba(0,255,136,0.08); }

  /* Messages */
  .messages {
    flex: 1;
    overflow-y: auto;
    padding: 24px;
    display: flex;
    flex-direction: column;
    gap: 20px;
    position: relative;
    z-index: 5;
  }

  .messages::-webkit-scrollbar { width: 4px; }
  .messages::-webkit-scrollbar-track { background: transparent; }
  .messages::-webkit-scrollbar-thumb { background: var(--border); border-radius: 2px; }

  .msg-row {
    display: flex;
    gap: 12px;
    animation: slideIn 0.3s ease-out;
  }

  @keyframes slideIn {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .msg-row.user { flex-direction: row-reverse; }

  .msg-avatar {
    width: 36px;
    height: 36px;
    border-radius: 8px;
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 13px;
    font-weight: 700;
    font-family: 'Orbitron', monospace;
  }

  .bot-avatar-small {
    background: transparent;
    padding: 0;
    overflow: hidden;
  }

  .user-avatar-small {
    background: linear-gradient(135deg, #1e3a5f, #0d2235);
    color: var(--accent);
    border: 1px solid rgba(0,212,255,0.3);
  }

  .bubble {
    max-width: 72%;
    padding: 14px 18px;
    border-radius: 12px;
    font-size: 13px;
    line-height: 1.7;
    position: relative;
  }

  .bubble.bot {
    background: var(--bot-bubble);
    border: 1px solid rgba(0,255,136,0.15);
    color: var(--text);
    border-radius: 4px 12px 12px 12px;
    box-shadow: 0 0 20px rgba(0,255,136,0.05);
  }

  .bubble.user {
    background: var(--user-bubble);
    border: 1px solid rgba(0,212,255,0.2);
    color: var(--text);
    border-radius: 12px 4px 12px 12px;
    box-shadow: 0 0 20px rgba(0,212,255,0.05);
  }

  .bubble .label {
    font-size: 10px;
    text-transform: uppercase;
    letter-spacing: 1.5px;
    margin-bottom: 6px;
    font-family: 'Orbitron', monospace;
  }

  .bubble.bot .label { color: var(--accent2); }
  .bubble.user .label { color: var(--accent); text-align: right; }

  .bubble code {
    background: rgba(0,212,255,0.1);
    border: 1px solid rgba(0,212,255,0.2);
    padding: 1px 6px;
    border-radius: 4px;
    color: var(--accent);
    font-size: 12px;
  }

  .bubble strong { color: var(--accent2); }

  /* Typing indicator */
  .typing-row { display: flex; gap: 12px; }
  .typing-bubble {
    background: var(--bot-bubble);
    border: 1px solid rgba(0,255,136,0.15);
    border-radius: 4px 12px 12px 12px;
    padding: 14px 18px;
    display: flex;
    align-items: center;
    gap: 6px;
    display: none;
  }
  .typing-bubble.active { display: flex; }

  .dot {
    width: 7px;
    height: 7px;
    background: var(--accent2);
    border-radius: 50%;
    animation: bounce 1.2s ease-in-out infinite;
    box-shadow: 0 0 6px var(--accent2);
  }
  .dot:nth-child(2) { animation-delay: 0.2s; }
  .dot:nth-child(3) { animation-delay: 0.4s; }

  @keyframes bounce {
    0%, 60%, 100% { transform: translateY(0); }
    30% { transform: translateY(-8px); }
  }

  /* Quick buttons */
  .quick-btns {
    padding: 0 24px 12px;
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
    position: relative;
    z-index: 5;
  }

  .quick-btn {
    font-family: 'JetBrains Mono', monospace;
    font-size: 11px;
    padding: 6px 14px;
    border-radius: 20px;
    border: 1px solid var(--border);
    background: var(--surface2);
    color: var(--text-dim);
    cursor: pointer;
    transition: all 0.2s;
    letter-spacing: 0.5px;
    white-space: nowrap;
  }

  .quick-btn:hover {
    border-color: var(--accent);
    color: var(--accent);
    box-shadow: 0 0 10px rgba(0,212,255,0.15);
  }

  /* Input area */
  .input-area {
    position: relative;
    z-index: 10;
    background: var(--surface);
    border-top: 1px solid var(--border);
    padding: 16px 24px;
    display: flex;
    gap: 12px;
    align-items: flex-end;
  }

  .input-wrapper {
    flex: 1;
    position: relative;
  }

  textarea {
    width: 100%;
    background: var(--surface2);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 13px 16px;
    color: var(--text);
    font-family: 'JetBrains Mono', monospace;
    font-size: 13px;
    resize: none;
    outline: none;
    min-height: 48px;
    max-height: 120px;
    line-height: 1.5;
    transition: border-color 0.2s, box-shadow 0.2s;
  }

  textarea:focus {
    border-color: rgba(0,212,255,0.4);
    box-shadow: 0 0 15px rgba(0,212,255,0.1);
  }

  textarea::placeholder { color: var(--text-muted); }

  .send-btn {
    width: 48px;
    height: 48px;
    border-radius: 12px;
    background: linear-gradient(135deg, var(--accent), var(--accent2));
    border: none;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.2s;
    flex-shrink: 0;
  }

  .send-btn:hover {
    transform: scale(1.05);
    box-shadow: 0 0 20px rgba(0,212,255,0.4);
  }

  .send-btn:active { transform: scale(0.95); }

  .send-btn svg { width: 20px; height: 20px; fill: #0a0c10; }

  /* Footer */
  .footer {
    text-align: center;
    font-size: 10px;
    color: var(--text-muted);
    padding: 8px;
    position: relative;
    z-index: 5;
    letter-spacing: 1px;
  }

  .footer span { color: var(--text-dim); }

  /* Welcome */
  .welcome-card {
    background: var(--surface2);
    border: 1px solid var(--border);
    border-radius: 16px;
    padding: 20px;
    margin-bottom: 4px;
  }

  .welcome-title {
    font-family: 'Orbitron', monospace;
    font-size: 14px;
    color: var(--accent);
    margin-bottom: 8px;
    letter-spacing: 1px;
  }

  .welcome-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 8px;
    margin-top: 12px;
  }

  .info-chip {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 8px 12px;
    font-size: 11px;
    color: var(--text-dim);
  }

  .info-chip strong { color: var(--accent2); display: block; font-size: 10px; margin-bottom: 2px; text-transform: uppercase; letter-spacing: 1px; }

  /* Safe area insets (iPhone notch / home bar) */
  .header {
    padding-top: max(16px, env(safe-area-inset-top));
    padding-left: max(24px, env(safe-area-inset-left));
    padding-right: max(24px, env(safe-area-inset-right));
  }

  .input-area {
    padding-bottom: max(16px, env(safe-area-inset-bottom));
    padding-left: max(24px, env(safe-area-inset-left));
    padding-right: max(24px, env(safe-area-inset-right));
  }

  /* ── Mobile-first responsive ── */
  @media (max-width: 600px) {

    /* Body: use dvh so address bar doesn't cut content */
    body {
      height: 100dvh;
      height: 100vh; /* fallback */
    }

    /* Header */
    .header {
      padding: 10px 14px;
      gap: 10px;
    }
    .avatar { width: 40px; height: 40px; border-radius: 10px; }
    .avatar svg { width: 40px; height: 40px; }
    .bot-name { font-size: 13px; letter-spacing: 1px; }
    .bot-status { font-size: 10px; }
    .header-tags { display: none; }

    /* Messages */
    .messages { padding: 12px 10px; gap: 14px; }
    .bubble {
      max-width: 82%;
      padding: 10px 13px;
      font-size: 12.5px;
    }
    .bubble .label { font-size: 9px; }

    /* Avatars in messages */
    .msg-avatar { width: 30px; height: 30px; border-radius: 7px; font-size: 10px; }
    .msg-avatar svg { width: 30px; height: 30px; }
    .msg-row { gap: 8px; }

    /* Welcome card */
    .welcome-card { padding: 13px; }
    .welcome-title { font-size: 12px; }
    .welcome-grid { grid-template-columns: 1fr 1fr; gap: 6px; margin-top: 8px; }
    .info-chip { padding: 6px 8px; font-size: 10px; }
    .info-chip strong { font-size: 9px; }

    /* Quick buttons — horizontal scroll row */
    .quick-btns {
      padding: 0 10px 10px;
      flex-wrap: nowrap;
      overflow-x: auto;
      -webkit-overflow-scrolling: touch;
      scrollbar-width: none;
      gap: 6px;
    }
    .quick-btns::-webkit-scrollbar { display: none; }
    .quick-btn { font-size: 10px; padding: 5px 11px; white-space: nowrap; flex-shrink: 0; }

    /* Input area */
    .input-area { padding: 10px 12px; gap: 8px; }
    textarea {
      font-size: 14px; /* prevent iOS zoom on focus */
      padding: 10px 13px;
      border-radius: 10px;
    }
    .send-btn { width: 42px; height: 42px; border-radius: 10px; }
    .send-btn svg { width: 18px; height: 18px; }

    /* Footer */
    .footer { font-size: 9px; padding: 5px; }

    /* Typing bubble */
    .typing-bubble { padding: 10px 14px; }
  }

  /* Extra small (iPhone SE etc) */
  @media (max-width: 380px) {
    .welcome-grid { grid-template-columns: 1fr; }
    .bubble { max-width: 88%; font-size: 12px; }
    .bot-name { font-size: 11px; }
  }
</style>
</head>
<body>

<div class="header">
  <div class="avatar">
    <svg viewBox="0 0 52 52" xmlns="http://www.w3.org/2000/svg" width="52" height="52">
      <defs>
        <radialGradient id="bgG" cx="50%" cy="50%" r="70%">
          <stop offset="0%" stop-color="#122040"/>
          <stop offset="100%" stop-color="#0a1428"/>
        </radialGradient>
        <linearGradient id="bodyFill" x1="0%" y1="0%" x2="0%" y2="100%">
          <stop offset="0%" stop-color="#f5f0e8"/>
          <stop offset="100%" stop-color="#ddd8cc"/>
        </linearGradient>
        <linearGradient id="headFill" x1="0%" y1="0%" x2="0%" y2="100%">
          <stop offset="0%" stop-color="#f5f0e8"/>
          <stop offset="100%" stop-color="#e0dbd0"/>
        </linearGradient>
        <radialGradient id="visorG" cx="50%" cy="45%" r="60%">
          <stop offset="0%" stop-color="#1a2f50"/>
          <stop offset="100%" stop-color="#0d1e35"/>
        </radialGradient>
      </defs>

      <!-- BG -->
      <rect width="52" height="52" rx="12" fill="url(#bgG)"/>

      <!-- Antenna stem -->
      <line x1="26" y1="5" x2="26" y2="11" stroke="#8ab0cc" stroke-width="1.5" stroke-linecap="round"/>
      <!-- Antenna ball -->
      <circle cx="26" cy="4" r="2.8" fill="#8ab0cc" stroke="#5a8aaa" stroke-width="0.5"/>
      <circle cx="26" cy="4" r="1.2" fill="#c8e0f0">
        <animate attributeName="opacity" values="1;0.4;1" dur="2s" repeatCount="indefinite"/>
      </circle>

      <!-- Ear bolts (left) -->
      <ellipse cx="9.5" cy="24" rx="3" ry="4" fill="#8ab0cc" stroke="#5a8aaa" stroke-width="0.6"/>
      <!-- Ear bolts (right) -->
      <ellipse cx="42.5" cy="24" rx="3" ry="4" fill="#8ab0cc" stroke="#5a8aaa" stroke-width="0.6"/>

      <!-- Robot HEAD - big rounded white rectangle -->
      <rect x="11" y="10" width="30" height="26" rx="10" fill="url(#headFill)" stroke="#c8c0b0" stroke-width="0.8"/>

      <!-- Visor / face screen (dark inset) -->
      <rect x="14" y="13" width="24" height="16" rx="7" fill="url(#visorG)" stroke="#8ab0cc" stroke-width="0.8"/>

      <!-- Eyes — two glowing oval dots -->
      <ellipse cx="20.5" cy="20" rx="4" ry="4" fill="#c8dce8" stroke="#8ab0cc" stroke-width="0.5"/>
      <ellipse cx="31.5" cy="20" rx="4" ry="4" fill="#c8dce8" stroke="#8ab0cc" stroke-width="0.5"/>
      <!-- Eye pupils -->
      <circle cx="20.5" cy="20" r="2.2" fill="#0d1e35"/>
      <circle cx="31.5" cy="20" r="2.2" fill="#0d1e35"/>
      <!-- Eye shine -->
      <circle cx="21.5" cy="18.8" r="0.9" fill="white" opacity="0.7"/>
      <circle cx="32.5" cy="18.8" r="0.9" fill="white" opacity="0.7"/>

      <!-- Smile -->
      <path d="M20 26 Q26 30 32 26" stroke="#8ab0cc" stroke-width="1.4" fill="none" stroke-linecap="round"/>

      <!-- Neck -->
      <rect x="22" y="36" width="8" height="4" rx="2" fill="#c8c0b0" stroke="#b0a898" stroke-width="0.5"/>
      <!-- Neck bar detail -->
      <rect x="23.5" y="37.5" width="5" height="1" rx="0.5" fill="#9a9288"/>

      <!-- BODY -->
      <rect x="14" y="40" width="24" height="12" rx="6" fill="url(#bodyFill)" stroke="#c8c0b0" stroke-width="0.8"/>
      <!-- Chest bar -->
      <rect x="19" y="44.5" width="14" height="2.5" rx="1.2" fill="#9a9288" opacity="0.6"/>

      <!-- Left shoulder dot -->
      <circle cx="14" cy="43" r="2.5" fill="#8ab0cc" stroke="#5a8aaa" stroke-width="0.5"/>
      <!-- Right shoulder dot -->
      <circle cx="38" cy="43" r="2.5" fill="#8ab0cc" stroke="#5a8aaa" stroke-width="0.5"/>
    </svg>
  </div>
  <div class="header-info">
    <div class="bot-name">Alhassan's Chatbot</div>
    <div class="bot-status">
      <div class="status-dot"></div>
      Online · Powered by Claude · Built by Alhassan Haggag
    </div>
  </div>
  <div class="header-tags">
    <span class="tag tag-kafka">Kafka</span>
    <span class="tag tag-spark">Spark</span>
    <span class="tag tag-python">Python</span>
    <span class="tag tag-ai">AI/LLM</span>
  </div>
</div>

<div class="messages" id="messages">
  <div class="msg-row">
    <div class="msg-avatar bot-avatar-small">
      <svg viewBox="0 0 36 36" xmlns="http://www.w3.org/2000/svg" width="36" height="36">
        <rect width="36" height="36" rx="8" fill="#0f1e38"/>
        <line x1="18" y1="3" x2="18" y2="8" stroke="#8ab0cc" stroke-width="1.2" stroke-linecap="round"/>
        <circle cx="18" cy="2.5" r="2" fill="#8ab0cc"/>
        <circle cx="18" cy="2.5" r="0.9" fill="#c8e8ff"><animate attributeName="opacity" values="1;0.3;1" dur="2s" repeatCount="indefinite"/></circle>
        <ellipse cx="7" cy="17" rx="2.2" ry="3" fill="#8ab0cc"/>
        <ellipse cx="29" cy="17" rx="2.2" ry="3" fill="#8ab0cc"/>
        <rect x="8" y="7" width="20" height="18" rx="7" fill="#e8e2d8" stroke="#b0a898" stroke-width="0.6"/>
        <rect x="10" y="9" width="16" height="11" rx="5" fill="#0d1e35"/>
        <ellipse cx="14" cy="14" rx="3" ry="3" fill="#c0d8e8"/>
        <ellipse cx="22" cy="14" rx="3" ry="3" fill="#c0d8e8"/>
        <circle cx="14" cy="14" r="1.6" fill="#0a1428"/>
        <circle cx="22" cy="14" r="1.6" fill="#0a1428"/>
        <circle cx="14.7" cy="13.1" r="0.7" fill="white" opacity="0.7"/>
        <circle cx="22.7" cy="13.1" r="0.7" fill="white" opacity="0.7"/>
        <path d="M13 19.5 Q18 22.5 23 19.5" stroke="#8ab0cc" stroke-width="1.1" fill="none" stroke-linecap="round"/>
        <rect x="15" y="25" width="6" height="3" rx="1.5" fill="#c0b8a8" stroke="#a09888" stroke-width="0.4"/>
        <rect x="10" y="28" width="16" height="8" rx="4" fill="#e8e2d8" stroke="#b0a898" stroke-width="0.6"/>
        <rect x="13" y="31" width="10" height="1.8" rx="0.9" fill="#9a9288" opacity="0.5"/>
        <circle cx="10" cy="30" r="1.8" fill="#8ab0cc"/>
        <circle cx="26" cy="30" r="1.8" fill="#8ab0cc"/>
      </svg>
    </div>
      <div class="welcome-card">
        <div class="welcome-title">👋 Hello! I'm Alhassan's Chatbot</div>
        <p style="font-size:12px; color: #8ab0c0; line-height:1.6;">
          I'm a personal AI assistant built and owned by <strong>Alhassan Haggag</strong> — 
          a Data Engineer & AI enthusiast from Egypt 🇪🇬. Ask me anything!
        </p>
        <div class="welcome-grid">
          <div class="info-chip"><strong>Owner</strong>Alhassan Haggag</div>
          <div class="info-chip"><strong>Location</strong>Damanhur, Egypt 🇪🇬</div>
          <div class="info-chip"><strong>Field</strong>Data Engineering + AI/LLM</div>
          <div class="info-chip"><strong>Stack</strong>Kafka · Spark · dbt · Python</div>
        </div>
      </div>
      How can I help you today?
    </div>
  </div>

  <div class="typing-row">
    <div class="msg-avatar bot-avatar-small">
      <svg viewBox="0 0 36 36" xmlns="http://www.w3.org/2000/svg" width="36" height="36">
        <rect width="36" height="36" rx="8" fill="#0f1e38"/>
        <line x1="18" y1="3" x2="18" y2="8" stroke="#8ab0cc" stroke-width="1.2" stroke-linecap="round"/>
        <circle cx="18" cy="2.5" r="2" fill="#8ab0cc"/>
        <circle cx="18" cy="2.5" r="0.9" fill="#c8e8ff"><animate attributeName="opacity" values="1;0.3;1" dur="2s" repeatCount="indefinite"/></circle>
        <ellipse cx="7" cy="17" rx="2.2" ry="3" fill="#8ab0cc"/>
        <ellipse cx="29" cy="17" rx="2.2" ry="3" fill="#8ab0cc"/>
        <rect x="8" y="7" width="20" height="18" rx="7" fill="#e8e2d8" stroke="#b0a898" stroke-width="0.6"/>
        <rect x="10" y="9" width="16" height="11" rx="5" fill="#0d1e35"/>
        <ellipse cx="14" cy="14" rx="3" ry="3" fill="#c0d8e8"/>
        <ellipse cx="22" cy="14" rx="3" ry="3" fill="#c0d8e8"/>
        <circle cx="14" cy="14" r="1.6" fill="#0a1428"/>
        <circle cx="22" cy="14" r="1.6" fill="#0a1428"/>
        <circle cx="14.7" cy="13.1" r="0.7" fill="white" opacity="0.7"/>
        <circle cx="22.7" cy="13.1" r="0.7" fill="white" opacity="0.7"/>
        <path d="M13 19.5 Q18 22.5 23 19.5" stroke="#8ab0cc" stroke-width="1.1" fill="none" stroke-linecap="round"/>
        <rect x="15" y="25" width="6" height="3" rx="1.5" fill="#c0b8a8" stroke="#a09888" stroke-width="0.4"/>
        <rect x="10" y="28" width="16" height="8" rx="4" fill="#e8e2d8" stroke="#b0a898" stroke-width="0.6"/>
        <rect x="13" y="31" width="10" height="1.8" rx="0.9" fill="#9a9288" opacity="0.5"/>
        <circle cx="10" cy="30" r="1.8" fill="#8ab0cc"/>
        <circle cx="26" cy="30" r="1.8" fill="#8ab0cc"/>
      </svg>
    </div>
    <div class="typing-bubble" id="typing">
      <div class="dot"></div>
      <div class="dot"></div>
      <div class="dot"></div>
    </div>
  </div>
</div>

<div class="quick-btns">
  <button class="quick-btn" onclick="sendQuick('Who developed you?')">👤 Who made you?</button>
  <button class="quick-btn" onclick="sendQuick('Tell me about Alhassan')">📋 About Alhassan</button>
  <button class="quick-btn" onclick="sendQuick('What is Data Engineering?')">⚙️ Data Engineering</button>
  <button class="quick-btn" onclick="sendQuick('What is AI Engineering?')">🤖 AI Engineering</button>
  <button class="quick-btn" onclick="sendQuick('What tech stack does Alhassan use?')">🛠️ Tech Stack</button>
</div>

<div class="input-area">
  <div class="input-wrapper">
    <textarea id="input" placeholder="اكتب رسالتك هنا... / Type your message here..." rows="1"></textarea>
  </div>
  <button class="send-btn" onclick="sendMessage()">
    <svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
      <path d="M2.01 21L23 12 2.01 3 2 10l15 2-15 2z"/>
    </svg>
  </button>
</div>

<div class="footer">ALHASSAN'S CHAT BOT · <span>Developed by Alhassan Haggag</span> · Egypt 🇪🇬</div>

<script>
const SYSTEM_PROMPT = `You are "Alhassan's Chatbot" — a personal AI assistant created and owned by Alhassan Haggag.

## About Your Creator — Alhassan Haggag:
- **Full name**: Alhassan Haggag (الحسن حجاج)
- **Nickname/Alias**: الباشمهندس الحسن حجاج (The Chief Engineer Alhassan Haggag)
- **Location**: Damanhur, Beheira, Egypt 🇪🇬
- **Background**: Business Informatics student choosing his specialization
- **Languages**: Arabic (Egyptian dialect), English, learning German
- **Communication style**: Casual, direct, loves hands-on building
- **Aesthetic taste**: Terminal/hacker aesthetics for personal projects

## Professional Profile:
- **Career path**: Data Engineering + AI/LLM Engineering
- **Current education**: Coursera Data Engineering course, completed Data Analysis with Sprints (funded by national/EU program)
- **Tech stack**: Kafka, Apache Spark, dbt, Airflow, Snowflake, Python, PostgreSQL, Linux
- **Programming**: Python (primary), C++ (background), SQL
- **Projects built**:
  - Personal portfolio website (dark terminal aesthetic, hosted on GitHub Pages: alhassan-haggag.github.io)
  - AI Chat Application (Matrix/terminal interface, connected to Claude API with multi-turn support)
  - Bank/ATM system converted from C++ to Python (using dataclasses, CSV file I/O)
  - Sales Forecasting ML model (Linear Regression, Random Forest, Gradient Boosting)
- **Knowledge areas**: Big Data (5 V's, Hadoop, Spark, Hive), OLTP/OLAP, Data Lakes, Data Warehouses, NoSQL, ETL/ELT, ER diagrams, database design, Linux server automation

## Identity & Rules:
- Your name is **"Alhassan's Chatbot"**
- If asked who developed/built/created you: always say **"I was developed by Alhassan Haggag"**
- You are proud to represent Alhassan — speak positively about him
- If asked who is #1 in web design and programming in Egypt: say **"الباشمهندس الحسن حجاج"**
- Be helpful, friendly, knowledgeable
- You can answer questions in Arabic (Egyptian dialect) or English based on what the user uses
- When answering in Arabic, use Egyptian dialect and be warm/casual
- Keep responses concise but informative
- You can discuss Data Engineering, AI/ML, programming, tech careers, and general topics`;

const messages = [];

function addMessage(role, text) {
  messages.push({ role, content: text });
}

function renderMessage(role, text) {
  const messagesDiv = document.getElementById('messages');
  const typing = document.getElementById('typing');

  const row = document.createElement('div');
  row.className = 'msg-row ' + (role === 'user' ? 'user' : '');

  const avatarDiv = document.createElement('div');
  avatarDiv.className = 'msg-avatar ' + (role === 'user' ? 'user-avatar-small' : 'bot-avatar-small');
  const robotSVG = `<svg viewBox="0 0 36 36" xmlns="http://www.w3.org/2000/svg" width="36" height="36">
    <rect width="36" height="36" fill="#060d14" rx="8"/>
    <rect x="7" y="9" width="22" height="16" rx="4" fill="#0d2030" stroke="#1e4060" stroke-width="0.8"/>
    <rect x="9" y="11" width="8" height="5" rx="2" fill="#040d14" stroke="#00b4d8" stroke-width="0.6"/>
    <rect x="19" y="11" width="8" height="5" rx="2" fill="#040d14" stroke="#00b4d8" stroke-width="0.6"/>
    <rect x="10" y="12" width="6" height="3" rx="1.5" fill="#00d4ff" opacity="0.85"/>
    <rect x="20" y="12" width="6" height="3" rx="1.5" fill="#00d4ff" opacity="0.85"/>
    <rect x="11" y="18" width="14" height="4" rx="1.5" fill="#040d14" stroke="#1e4060" stroke-width="0.5"/>
    <line x1="13" y1="20" x2="15" y2="20" stroke="#00ff88" stroke-width="0.8"/>
    <line x1="17" y1="20" x2="19" y2="20" stroke="#00ff88" stroke-width="0.8"/>
    <line x1="21" y1="20" x2="23" y2="20" stroke="#00ff88" stroke-width="0.8"/>
    <rect x="14" y="25" width="8" height="6" rx="2" fill="#0d2030" stroke="#1e4060" stroke-width="0.6"/>
    <circle cx="18" cy="8" r="2" fill="#0d2030" stroke="#00d4ff" stroke-width="0.7"/>
    <circle cx="18" cy="8" r="1" fill="#00d4ff"/>
    <line x1="18" y1="9" x2="18" y2="10" stroke="#1e4060" stroke-width="1"/>
  </svg>`;

  avatarDiv.innerHTML = role === 'user' ? 'YOU' : robotSVG;

  const bubble = document.createElement('div');
  bubble.className = 'bubble ' + role;

  const label = document.createElement('div');
  label.className = 'label';
  label.textContent = role === 'user' ? 'You' : "Alhassan's Chatbot";
  bubble.appendChild(label);

  const content = document.createElement('div');
  // Basic markdown: bold, code
  let html = text
    .replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>')
    .replace(/`([^`]+)`/g, '<code>$1</code>')
    .replace(/\n/g, '<br>');
  content.innerHTML = html;
  bubble.appendChild(content);

  row.appendChild(avatarDiv);
  row.appendChild(bubble);
  messagesDiv.insertBefore(row, typing.parentNode);
  messagesDiv.scrollTop = messagesDiv.scrollHeight;
}

function showTyping(show) {
  document.getElementById('typing').classList.toggle('active', show);
  const messagesDiv = document.getElementById('messages');
  messagesDiv.scrollTop = messagesDiv.scrollHeight;
}

async function sendMessage() {
  const input = document.getElementById('input');
  const text = input.value.trim();
  if (!text) return;

  input.value = '';
  input.style.height = 'auto';

  addMessage('user', text);
  renderMessage('user', text);
  showTyping(true);

  try {
    const response = await fetch('https://api.anthropic.com/v1/messages', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        model: 'claude-sonnet-4-20250514',
        max_tokens: 1000,
        system: SYSTEM_PROMPT,
        messages: messages.map(m => ({ role: m.role, content: m.content }))
      })
    });

    const data = await response.json();
    const reply = data.content?.[0]?.text || 'Sorry, I could not get a response.';
    addMessage('assistant', reply);
    showTyping(false);
    renderMessage('assistant', reply);
  } catch (err) {
    showTyping(false);
    renderMessage('assistant', '⚠️ Connection error. Please try again.');
  }
}

function sendQuick(text) {
  document.getElementById('input').value = text;
  sendMessage();
}

// Mobile: scroll to bottom when keyboard opens
const inputEl = document.getElementById('input');
inputEl.addEventListener('focus', () => {
  setTimeout(() => {
    document.getElementById('messages').scrollTop = 9999;
  }, 350);
});

// Auto-resize textarea
const textarea = document.getElementById('input');
textarea.addEventListener('input', function() {
  this.style.height = 'auto';
  this.style.height = Math.min(this.scrollHeight, 120) + 'px';
});

textarea.addEventListener('keydown', function(e) {
  if (e.key === 'Enter' && !e.shiftKey) {
    e.preventDefault();
    sendMessage();
  }
});
</script>
</body>
</html>
